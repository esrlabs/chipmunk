namespace binding;

table MulticastInfo {
    multiaddr: string;
    interface: string;
}

table UDPTransportConfig {
    bind_addr: string;
    multicast: [MulticastInfo];
}

root_type UDPTransportConfig;
