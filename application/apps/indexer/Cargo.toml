[workspace]

members = [
    "indexer_base",
    "indexer_cli",
    "matcher",
    "merging",
    "parsers",
    "processor",
    "session",
    "sources",
]

# only uncomment when profiling
[profile.release]
debug = true
