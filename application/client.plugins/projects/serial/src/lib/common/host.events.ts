export enum EHostEvents {
    connected = 'connected',
    disconnected = 'disconnected',
    error = 'error',
    state = 'state'
}

export enum EHostCommands {
    open = 'open',
    close = 'close',
    list = 'list',
}
