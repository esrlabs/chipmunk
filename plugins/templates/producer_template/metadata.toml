# This is the template of producer plugin metadata file which should placed in the plugin directory 
# and renamed to the plugin's name.
name = "Producer Template"
description = "Optional description of the producer plugin"
