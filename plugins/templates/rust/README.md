# Plugin Templates in Rust

This directory contains ready-to-use templates for Chipmunk plugins written in Rust. 
Plugin developers can simply copy a template directory to quickly start a new plugin project.
