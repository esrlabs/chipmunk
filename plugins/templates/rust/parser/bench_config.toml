# Configuration required to run the parser in Chipmunk benchmarks.

# Path to the plugin WebAssembly (WASM) file.
# Can be absolute or relative to the `crates/core/processor` directory.
binary_path = "../../../plugins/templates/rust/parser/target/wasm32-wasip1/release/parser_template.wasm"

# Plugin-specific configurations:

[[config]]
id = "my_bool_config"
value.Boolean = true

[[config]]
id = "my_text_config"
value.Text = "Text Config Value"

