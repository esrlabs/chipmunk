interface Marker{
    value           : string,
    foregroundColor : string,
    backgroundColor : string,
    active          : boolean,
    lineIsTarget    : boolean,
    isRegExp        : boolean
}

export { Marker };
